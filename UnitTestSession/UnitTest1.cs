using Microsoft.VisualStudio.TestTools.UnitTesting;
//using Session2;
using System;

namespace UnitTestSession
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void MinLength5()
        {
            //var mainWindow = new MainWindow(); // Предполагается, что у вас есть класс MainWindow в проекте Session2.1
            //var passTextBox = mainWindow.PassTextBox;
            //passTextBox.Text = "1234"; // Установка значения с менее чем 5 символами

            // Assert
            //Assert.IsTrue(passTextBox.Text.Length >= 5,
        }
    }
}
